import { TimelineItemDirective } from './timeline-item.directive';

describe('TimelineItemDirective', () => {
  it('should create an instance', () => {
    const directive = new TimelineItemDirective();
    expect(directive).toBeTruthy();
  });
});
